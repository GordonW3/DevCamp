<%= link_to 'blogs', blogs_path if logged_in?(:site_admin) %> /
	<%= link_to 'Edit', edit_blog_path(@blog) if logged_in?(:site_admin) %> /
	<%= link_to 'Delete', @blog, method: :delete, data: { confirm: 'Are you sure you want to delete this?'} if logged_in?(:site_admin) %> 